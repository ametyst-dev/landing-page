# Develop — Memory

## Learnings

### From sprint-0004 (2026-04-17)
- **Commit sprint-memory.md immediately**: Repo agents should commit `brainstormings/sprint-*/sprint-memory.md` updates at the end of each step (don't leave them uncommitted). Sprint 0004 had repeated `git checkout` conflicts when HIL pulled the sprint branch because sprint-memory had uncommitted diffs on the sub-branch.
